# .env.example
# ローカル開発用の環境変数サンプル。実際の値は .env に入れ、.env は .gitignore に含めてください。

# ----------------------------
# X / Twitter API credentials
# ----------------------------
# OAuth1.0a (User context) 用のキー/トークン
API_KEY=your_consumer_key               # Consumer Key (API Key)
API_SECRET=your_consumer_secret         # Consumer Secret (API Secret)
ACCESS_TOKEN=your_access_token          # Access Token (ユーザー用)
ACCESS_TOKEN_SECRET=your_access_token_secret  # Access Token Secret

# ----------------------------
# Bot 設定（調整可能）
# ----------------------------
# KEYWORDS: 正規表現 (パイプ '|' で OR を表現)
# 例: 歌枠|歌ってみた|cover|カバー
KEYWORDS=歌枠|歌ってみた|cover|カバー

# 1回のAPI呼び出しで取得する最大ツイート数（Tweepy/Twitter の制約に基づく）
# 値が大きいほど1回で多く読み取るが、"posts consumption" などのカウントが増える可能性あり。
POLL_MAX_RESULTS=50

# 実行ごとの最大リツイート件数（安全のため小さめに設定）
# DRY_RUN=true の間は実際のRTは行われないので、確認後に上げてください。
MAX_RETWEETS_PER_RUN=5

# DRY_RUN: 'true' のときはリツイートを実行しない（ログのみ）。
DRY_RUN=true

# 実行間隔に依存する時間判定（単位: 時間）
# 例：3（=直近 3 時間内の投稿のみを対象）
LOOKBACK_HOURS=12

# ログレベル（DEBUG/INFO/WARNING/ERROR）
LOG_LEVEL=INFO
