# .env.example
# ローカル環境で動作させるための環境変数テンプレート
# 実際の値は .env に設定し、.env は .gitignore に含めて下さい

# ----------------------------
# X / Twitter API 認証情報
# ----------------------------
API_KEY=your_consumer_key
API_SECRET=your_consumer_secret
ACCESS_TOKEN=your_access_token
ACCESS_TOKEN_SECRET=your_access_token_secret

# ----------------------------
# Bot 設定
# ----------------------------
# キーワード (正規表現形式)：いずれかにマッチすれば対象となる
KEYWORDS=歌枠|歌ってみた|cover|カバー

# タイムライン取得時のツイート最大件数
POLL_MAX_RESULTS=50

# 1 回の実行で行う最大リツイート件数（安全側に小さめに設定）
MAX_RETWEETS_PER_RUN=1

# DRY_RUN=true の場合は実際のリツイートを行わずログ出力だけにするモード
DRY_RUN=true

# 投稿時間判定範囲（単位：時間）— この範囲内に投稿されたツイートのみ対象
LOOKBACK_HOURS=3

# ログレベル：DEBUG, INFO, WARNING, ERROR のいずれか
# INFO：実行状況の要点を出力。DEBUG：詳細ログ出力。
LOG_LEVEL=INFO

# state.json ファイル名（ローカル保存用。GitHub Actions では永続化されない点に注意）
STATE_FILE=state.json
